import { FunctionalComponent } from "preact/src/index.d.ts";

const Head: FunctionalComponent = () => {
    return(
        <div>
            <a href={`/`}>Home</a>
        </div>
    );
}

export default Head;